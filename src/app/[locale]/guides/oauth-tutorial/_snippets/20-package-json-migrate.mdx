```json
{
  "scripts": {
    // Update `dev` to run migrations:
    "dev": "pnpm migrate && next dev",
    "build": "next build",
    "start": "pnpm migrate && next start",
    "lint": "eslint",
    // And add this line:
    "migrate": "tsx scripts/migrate.ts",
  }
}
```
