import {Container} from "@/components/Container"
import setstatus from "./set-status.png"
import tap from "./tap-status.png"
import feed from "./feed-status.png"
import taproot from "./taproot-status.png"
import S01 from './_snippets/01-install-brew.mdx'
import S02 from './_snippets/02-pnpm-add.mdx'
import S03 from './_snippets/03-npm-install-lex.mdx'
import S04 from './_snippets/04-lex-install.mdx'
import S05 from './_snippets/05-lex-build.mdx'
import S06 from './_snippets/06-scope-constant.mdx'
import S07 from './_snippets/07-db-schema.mdx'
import S08 from './_snippets/08-db-migrations.mdx'
import S09 from './_snippets/09-status-route.mdx'
import S10 from './_snippets/10-status-picker.mdx'
import S11 from './_snippets/11-page-v1.mdx'
import S12 from './_snippets/12-tap-index.mdx'
import S13 from './_snippets/13-db-queries-v1.mdx'
import S14 from './_snippets/14-webhook-route.mdx'
import S15 from './_snippets/15-page-status-update.mdx'
import S16 from './_snippets/16-tap-run.mdx'
import S17 from './_snippets/17-curl-add-did.mdx'
import S18 from './_snippets/18-queries-extra-imports.mdx'
import S19 from './_snippets/19-queries-handle-statuses.mdx'
import S20 from './_snippets/20-page-v2.mdx'
import S21 from './_snippets/21-tap-run-signal.mdx'

export const header = {
  title: '소셜 앱 만들기',
  description: 'AT Record Lexicon을 사용하여 상태 업데이트를 브로드캐스트하고 수신하는 앱을 만들어 보세요.',
}

이 튜토리얼에서는 사용자 정의 어휘집(Lexicon)과 실시간 동기화를 활용하여 상태 설정 앱을 구축합니다.
이 튜토리얼의 소스 코드와 다른 예제 프로젝트는 [Cookbook](https://github.com/bluesky-social/cookbook/) 저장소의 `statusphere` 디렉터리에서 확인할 수 있습니다.
## 필수 조건
본 튜토리얼은 [NextJS를 활용한 OAuth 튜토리얼](/guides/oauth-tutorial)에서 생성한 앱을 기반으로 합니다. 시작하기 전에 NextJS OAuth 앱을 완성해 두셔야 합니다. 해당 단계를 건너뛰고 싶다면 [Cookbook](https://github.com/bluesky-social/nextjs-oauth-tutorial/tree/129cf28b4146ae709e0dac43ce20154ab5237559)에서 완성된 OAuth 앱을 복제할 수 있습니다.
다음이 설치되어 있어야 합니다:
- Node.js 18 이상
- Go 1.25 이상
- pnpm 패키지 관리자
[homebrew](https://brew.sh/)가 지원하는 플랫폼에서는 다음 명령어로 설치할 수 있습니다:

<S01 />

기존 프로젝트에 다음 Atproto 라이브러리를 추가하세요:

<S02 />

또한 `lex` CLI 도구를 전역으로 설치해야 합니다:

<S03 />

프로젝트에 어휘집(Lexicon)을 추가하는 것으로 시작합니다.
## 파트 1: 어휘집
[어휘집](/guides/lexicon)은 Atproto 내 레코드의 스키마를 정의합니다. 본 튜토리얼을 위해 저희는 이미 새로운 "Statusphere" Lexicon을 `xyz.statusphere.status`에서 확인할 수 있습니다. `lex`를 사용하여 이 Lexicon을 다운로드하고 프로젝트에 빌드할 수 있습니다:

<S04 />

원한다면 다운로드된 Lexicon 파일을 `lib/lexicons/ xyz.statusphere.status.json`에서 확인할 수 있습니다. 여기서 `lex build`를 실행하면 설치된 모든 레식콘에 대한 TypeScript 타입을 생성할 수 있습니다:

<S05 />

<Note>
필요하다면 이 명령어를 `package.json`에 추가할 수 있지만, 사전이 빌드된 상태에서 이 튜토리얼에서는 더 이상 사용하지 않을 것입니다. `lex install`로 프로젝트에 다른 사전을 추가할 경우, ` --override` 플래그를 사용하여 `lex build`를 다시 실행해야 합니다.
</Note>
지금 해야 할 한 가지 더는 `lib/auth/client.ts` 파일의 `SCOPE` 상수를 업데이트하여 이 컬렉션에 대한 액세스를 요청하는 것입니다. Atproto 권한 범위는 Lexicons이므로 앱이 요청하는 범위 목록에 `xyz.statusphere.status`를 추가할 수 있습니다:

<S06 />

이제 데이터베이스 스키마 생성을 진행할 수 있습니다.
## 파트 2: 데이터베이스 스키마
`lib/db/index.ts`를 업데이트하여 새 테이블을 추가하세요:

<S07 />

다음으로, 이 테이블들을 생성하는 마이그레이션 스크립트를 추가합니다. OAuth 튜토리얼을 기반으로 구축 중이고 이미 데이터베이스를 배포/마이그레이션한 경우, 이들을 `lib/db/migrations.ts`에 "002"와 같은 새 마이그레이션으로 넣어야 합니다:

<S08 />

`pnpm migrate`를 실행하여 이러한 새로운 마이그레이션을 데이터베이스에 적용할 수 있습니다. 이제 실제 앱 로직을 구축해 보겠습니다!
## 파트 3: 상태 제출
먼저 사용자가 자신의 [PDS](/guides/the-at-stack#pds)에 상태를 작성할 수 있는 기능을 구축합니다. `app/api/status/route.ts`에 새 API 경로를 생성하세요:

<S09 />

이 코드는 사용자가 로그인했는지 확인하고, OAuth 세션을 사용해 lex `Client`를 생성한 후 제출된 상태 텍스트로 새로운 `xyz.statusphere.status` 레코드를 생성합니다.
다음으로 `components/StatusPicker.tsx`에 상태 선택기 컴포넌트를 생성하세요:

<S10 />

마지막으로 `app/page.tsx`를 업데이트하여 상태 선택기를 포함시킵니다:

<S11 />

`pnpm dev`로 앱을 실행하세요 — 로그인하고 이모지를 선택해 상태를 설정할 수 있습니다!
<Container>
  <Image src={setstatus} alt="" className="w-full max-w-md mx-auto" />
</Container>
이 앱에서는 아직 상태를 확인할 수 없지만, [atproto.at](https://atproto.at/)에서 계정을 조회하고 `xyz.statusphere.status` 어휘를 확인하여 이 기록을 볼 수 있습니다.
<Container>
  <Image src={taproot} alt="" className="w-full max-w-md mx-auto" />
</Container>
다음으로 상태 업데이트를 실제로 수신하기 위한 동기화 기능을 추가할 것입니다.
## 파트 4: Tap을 이용한 실시간 동기화
[Tap](/guides/backfilling#using-tap)은 AT Protocol 레코드를 동기화하고 스트리밍하는 가장 좋은 방법입니다. 데이터베이스의 새 레코드를 동기화하기 위해 Tap [TypeScript 라이브러리](https://www.npmjs.com/package/@atproto/tap)를 사용할 것입니다. `lib/tap/index.ts`를 생성하세요:

<S12 />

다음으로, Tap 이벤트 처리를 위한 데이터베이스 쿼리를 포함하는 `lib/db/queries.ts`를 생성합니다:

<S13 />

`setCurrStatus` 헬퍼는 사용자당 가장 최근 상태만 `current = 1`이 되도록 보장합니다. 이는 나중에 사용될 것입니다.
Tap은 `app/api/webhook/route.ts`의 웹훅 엔드포인트를 통해 이벤트를 전달합니다:

<S14 />

Tap은 네트워크 어디에서든 레코드가 변경될 때 웹훅 이벤트를 전송합니다. 요청은 공유 비밀 키를 사용하여 검증됩니다. `identity` 이벤트의 경우 계정 캐시를 업데이트하고, `record` 이벤트의 경우 레코드가 어휘집(Lexicon)과 일치하는지 확인한 후 데이터베이스에 삽입/업데이트/삭제합니다.
현재 사용자의 상태를 가져와 전달하도록 `app/page.tsx`를 다시 업데이트하세요:

<S15 />

이렇게 하면 페이지 로드 시 `StatusPicker`가 사용자의 현재 상태를 강조 표시합니다.
이제 다른 터미널에서 소스 저장소에서 `tap`을 설치하고 실행하세요:

<S16 />

실행 중인 앱을 Ctrl+C로 중지하세요. 그런 다음 추적을 위해 데이터 저장소를 Tap에 추가합니다(나중에 다른 데이터 소스를 추가할 예정입니다):

<S17 />

`pnpm dev`로 앱을 재시작하고 로그인 절차를 완료하면 저장된 상태가 강조 표시됩니다:
<Container>

<Image src={tap} alt="" className="w-full max-w-md mx-auto" />
</Container>
## 파트 5: 핸들 및 피드 표시
마지막으로 사용자 핸들과 모든 사용자의 상태 피드를 표시하는 기능을 추가합니다.
먼저 `lib/db/queries.ts`에 추가 임포트를 추가하세요:

<S18 />

그런 다음 계정 핸들과 상태를 가져오는 추가 함수를 추가합니다:

<S19 />

다음으로 `app/page.tsx`를 업데이트하여 상위 상태, 핸들, 타임스탬프가 포함된 전체 피드를 표시합니다:

<S20 />

이제 로그인한 사용자는 단순히 "로그인됨" 대신 "@사용자명 님으로 로그인됨"을 볼 수 있으며, 모든 사용자의 최근 상태 업데이트 피드와 함께 현재 가장 인기 있는 상태 업데이트를 보여주는 "인기 상태" 섹션도 확인할 수 있습니다.
`pnpm dev`로 앱을 중지하고 다시 시작할 수 있습니다.
마지막으로 한 가지 — 앞서 `tap`에 단일 데이터 소스만 추가했음을 기억하세요. 전체 네트워크의 상태를 보려면 `--signal-collection` 플래그와 함께 `tap`을 중지하고 재실행하세요:

<S21 />

 그러면 네트워크 내 다른 사용자들의 상태 메시지가 표시되기 시작합니다!
<Container>
  <Image src={feed} alt="" className="w-full max-w-md mx-auto" />
</Container>
## 결론
이제 OAuth로 로그인하고, 상태 레코드를 정의하는 커스텀 어휘(Lexicon)를 사용하며, Tap으로 레코드를 실시간 동기화하는 완전한 Atproto 앱을 구축했습니다.
이를 통해 AT Protocol 기반 개발을 계속하는 데 필요한 모든 도구를 확보했습니다. 더 많은 가이드와 튜토리얼은 저희 [가이드](/guides) 섹션에서 더 많은 가이드와 튜토리얼을, [쿡북](https://github.com/bluesky-social/cookbook/) 저장소에서 더 많은 예제 앱을 확인하실 수 있습니다. 즐거운 개발 되세요!
